# 任务 <编号>：<简短标题>

<!--
使用说明（填完删掉本段注释）：
- 全中文，含章节标题。公式 / 代码标识符 / 路径 / 命令 / 引用原文可保留原文。
- 必填节：状态、目标、技术路线、实现计划、交付物、复现入口、验收标准、Checkpoint 切片、阻塞与边界（含 Non-goals）。
- 可缩放节（背景、指标、备注）按任务轻重保留或精简；轻量写作类任务可省指标、压缩技术路线。
- 三道执行闸门嵌在模板里：验收标准每条绑定可执行「验证」（治谎报完成）、Checkpoint 切片（治漂移）、阻塞与边界的 Non-goals + 越界处置（治理解偏差/过度工程）。派活时配套附上「执行契约预设词」。
- 信息未定就写 TBD / 待定 并说明缺什么，不要编造路径或命令。
-->

## 状态

状态：规划中 | 进行中 | 阻塞 | 已完成 | 已废弃
最近更新：YYYY-MM-DD
一句话进展：<现在做到哪一步、下一步是什么>

## 目标

<这个子任务要完成什么、达成什么、边界在哪。一两句话让人看懂。>

## 背景

<为什么需要这个任务；依赖哪个上游任务、服务于哪个下游任务 / 论文 / 系统。轻量任务可省。>

## 技术路线

### 方法

<采用的技术 / 算法 / 模型 / simulator / real backend，以及关键设计取舍（为什么这么选）。>

### 输入

<依赖的数据 / checkpoint / 环境 config / schema / 接口。>

### 输出

<产生什么：checkpoint / 指标 / 轨迹 / 视频 / 日志 / 场景文件 / 改后文本等。>

## 实现计划

<代码实现在哪里、需要新增或改动什么。用表格列出真实代码位置：>

| 模块 / 文件 | 作用 | 状态 |
|---|---|---|
| `src/.../xxx.py` | <这个文件/模块负责什么> | 已完成 / 进行中 / 待办 |
| `scripts/xxx.py` | <脚本作用> | 待办 |

## 指标

<实验类任务必填；列出关心的指标并给出**定义**，保证跨 run 可比。纯写作类可整节省略。>

- `success_rate`：成功 episode 数 / 总 episode 数
- `<metric>`：<定义>

## 交付物

> 状态为「已完成」时必填。

| 交付物 | 路径 | 状态 | 说明 |
|---|---|---|---|
| <交付物名> | `runs/.../metrics.json` | 已完成 / 进行中 | <说明> |
| <代码/配置> | `src/.../xxx.py` | 已完成 | <说明> |

产出命令（生成上述交付物的实际命令，可直接粘贴运行）：

```bash
python scripts/train.py --config configs/xxx.yaml --seed 0
```

验证命令（查看 / 核对交付结果）：

```bash
python scripts/eval.py --run_dir runs/xxx
cat runs/xxx/eval/<eval_id>/metrics.json
```

## 复现入口

- 发起新 run：见 `../运行命令.md`
- 完整命令 / 代码版本 / 环境 / 数据：见对应 `runs/.../run_config.json`（复现的单一真相，本文件不抄录）
- 结果登记：见 `../实验结果.md`

## 验收标准

<每条都是可勾选、可客观判断的完成条件，且**每条内联一条可执行的「验证」命令**——命令产出能判真假的输出（测试 exit code / 对 `runs/<id>/metrics.json` 跑 jq 比阈值 / 复现命令产出指定 artifact）。"完成"的定义 = 每条的验证命令都给出注释里的期望输出。**禁止仅凭自述声称完成。** 不要写"做好为止"这类无法验证的空话。>

- [ ] <可客观判断的条件 1，如 AUC ≥ 0.8>
      验证: `rtk jq '.constraint_recovery_auc >= 0.8' runs/<run_id>/metrics.json`  # 期望 true
- [ ] <条件 2，如 rollout 可执行无碰撞>
      验证: `python scripts/check_rollout.py runs/<run_id>/trace.json`  # 期望 exit 0
- [ ] 可复现（本次实际命令 / 代码版本 / 环境 / 数据已写入对应 `run_config.json`）
      验证: `rtk jq -e '.git_commit and .cmd and .env' runs/<run_id>/run_config.json`  # 期望 exit 0
- [ ] 结果写入 `runs/` 并在 `实验结果.md` 登记
      验证: `rtk rg -q "<run_id>" docs/<project>/实验结果.md`  # 期望 exit 0

## Checkpoint 切片

<把任务拆成 2–5 个有序纵切片，每片绑一条验收标准 + 一个「报到点」（该停下时要产出什么 commit / 文件 / 输出）。
执行 agent 每做完一片就在报到点粘出证据：交互模式停下报到等确认，后台模式 commit + 写 `PROGRESS.md`。
小到能独立验证、看得见进展，避免一口气闷头跑偏。>

1. <切片名> → 满足验收标准 #1 → 报到点：commit + `runs/<id>/metrics.json` 出 AUC
2. <切片名> → 满足验收标准 #2 → 报到点：rollout trace 落盘 + check 脚本 exit 0

## 阻塞与边界

<必填。把"已有的、这次做的、还没做的"分开，并显式写出本任务的 Non-goals 范围栅栏与越界处置规则。>

- 已有底座：<现成能直接用的基础设施 / 代码>
- 本次落地：<这个任务这次做完的部分>
- 仍未做：<后续工作 / 明确超出本次范围的部分>

### 本任务不做（Non-goals）

<显式列出栅栏外的事，把"理解偏差/过度工程"在 spec 层就堵掉。越具体越好。>

- 不改 <X>；不引入 <Y>；不顺手重构 <Z>

### 越界处置

- 干活中发现栅栏外但有价值的事 → 追加到本文件 `## 开放问题`，**不要动手做**。

## 开放问题

<执行中冒出的、超出本任务范围或需要决策的问题往这里堆，供事后复盘 / 起新任务。初始可留空。>

- （初始为空）

## 备注

<关联决策、设计取舍、偏离计划之处（本想做 X、实际做了 Y、原因 Z）。
过时但有参考价值的内容用「（历史记录）」标注保留，不直接删。>
